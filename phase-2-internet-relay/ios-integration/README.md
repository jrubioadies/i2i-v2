# iOS Integration Notes

This folder is reserved for Swift-side phase 2 integration notes and drafts.

Initial integration target:

```text
TransportProtocol
├── MultipeerTransport
└── InternetRelayTransport
```

The first Swift milestone is to add `InternetRelayTransport` without changing the MVP local transport behavior.

